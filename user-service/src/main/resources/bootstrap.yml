spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: sasa
  cloud:
    config:
      enabled: true
      uri: http://localhots:8081